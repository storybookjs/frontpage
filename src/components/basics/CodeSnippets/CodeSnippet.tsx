import React from 'react';

export interface CodeSnippetProps {
  paths: string[];
  isTerminalSnippet?: boolean;
}

// TODO: Implement complex MDX codeSnippets with multiple paths
export const CodeSnippet = ({ paths, isTerminalSnippet }: CodeSnippetProps) => {
  return null;
};
